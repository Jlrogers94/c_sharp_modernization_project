[build-system]
requires = ["setuptools>=75", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "legacy-modernizer-agent"
version = "0.1.0"
description = "Local Python orchestration agent for incrementally modernizing large legacy C#/.NET applications with Gemini completions."
readme = "README.md"
requires-python = ">=3.11"
authors = [{name = "Jake Rogers"}]
dependencies = [
  "httpx>=0.27,<1",
  "tree-sitter>=0.23,<0.26",
  "tree-sitter-c-sharp>=0.23,<0.24",
]

[project.optional-dependencies]
dev = ["pytest>=8,<9"]

[project.scripts]
modernizer = "modernizer_agent.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
