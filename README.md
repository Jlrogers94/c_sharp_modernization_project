# C# Modernization Project

This repository hosts the local Python modernization agent used to incrementally modernize a legacy C#/.NET application while preserving existing TestComplete functional coverage.

The initial implementation is developed on a feature branch and merged by pull request.
